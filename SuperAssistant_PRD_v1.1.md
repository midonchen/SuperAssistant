# 超级私人助理 · LifeOS Assistant
## 产品需求文档 PRD v1.1
**MVP · 生活物资管理模块**

---

| 文档版本 | 文档状态 | 当前阶段 | 覆盖模块 |
|---------|---------|---------|---------|
| v1.1 | 待评审 | MVP · Phase 1 | 生活物资管理 |

---

## 目录

- [00 文档信息与变更记录](#00-文档信息与变更记录)
- [01 产品定位与价值主张](#01-产品定位与价值主张)
- [02 目标用户与核心场景](#02-目标用户与核心场景)
- [03 功能需求规格](#03-功能需求规格)
- [04 数据架构设计](#04-数据架构设计)
- [05 交互流程设计](#05-交互流程设计)
- [06 UI 设计规范](#06-ui-设计规范)
- [07 算法模型详细设计](#07-算法模型详细设计)
- [08 非功能需求](#08-非功能需求)
- [09 技术选型建议](#09-技术选型建议)
- [10 开发计划与工程治理（v1.1 新增）](#10-开发计划与工程治理v11-新增)
- [11 API 接口契约与鉴权规范（v1.1 新增）](#11-api-接口契约与鉴权规范v11-新增)
- [12 iOS 页面交互规格（v1.1 新增）](#12-ios-页面交互规格v11-新增)
- [13 管理后台 UI/UX 设计规范（v1.1 新增）](#13-管理后台-uiux-设计规范v11-新增)
- [14 测试与验收基线（v1.1 新增）](#14-测试与验收基线v11-新增)
- [15 风险评估与应对策略](#15-风险评估与应对策略)
- [16 版本迭代路线图](#16-版本迭代路线图)

---

## 00 文档信息与变更记录

| 版本 | 日期 | 变更内容 | 状态 |
|------|------|---------|------|
| v1.1 | 2026-Q1 | 新增开发前置工程治理规范，并补齐 API 契约、iOS 页面交互规格、管理后台 UI/UX 规范、测试与验收基线四大章节 | 待评审 |
| v1.0 | 2025-Q3 | 初始版本，MVP 功能范围定义，生活物资管理模块完整规格 | 草稿 |

> **⚠ 文档范围声明**  
> 本文档聚焦于 MVP 阶段——生活物资管理模块。职业规划、工作安排、家庭关系、社交管理等模块将在后续迭代版本中单独成文。

---

## 01 产品定位与价值主张

### 产品愿景

LifeOS Assistant 是一款面向都市人群的超级私人智能管家，致力于通过 AI 感知与自动化能力，将繁杂琐碎的生活管理、职业规划、工作安排、家庭维护和社交管理整合至单一入口，让用户无感管理生活的每一个维度。

**长期愿景：** 成为用户"数字生活的第二大脑"——比用户更了解自己的生活节律，提前预判需求，主动给出行动建议。

### MVP 范围与价值定位

> **MVP 聚焦：解决家庭物资管理的三大痛点**
>
> **① 录入繁琐** —— 购物后手动记录耗时费力，导致记录行为不可持续  
> **② 库存不清** —— 无法即时掌握各类物资剩余量，依赖记忆导致频繁遗漏  
> **③ 采购靠感觉** —— 买多了浪费，买少了断货，缺乏数据支撑的精准采购计划

| 维度 | 当前痛点 | MVP 解法 |
|------|---------|---------|
| 信息录入 | 手工输入繁琐，坚持率极低 | OCR 拍票 + 长按语音，两种零摩擦录入 |
| 库存感知 | 库存状态靠记忆，误差大 | AI 自动衰减模型 + 用户随时校准 |
| 采购决策 | 凭感觉买，经常不足或过剩 | 基于消耗模型的数据驱动采购建议 |
| 行动成本 | 决策到执行链路长 | 一键生成采购清单，直接分享或使用 |

### 竞品对比与差异化

| 产品 | 核心定位 | 不足 | 我们的差异化 |
|------|---------|------|------------|
| 厨房日记 / 保质期管家 | 食品管理记录 | 纯手工录入，无 AI 能力 | AI 语音 + OCR 零摩擦录入 |
| 京东购物清单 | 购物计划 | 需手动维护，无消耗追踪 | 自动消耗感知，主动推送建议 |
| Notion / 飞书 | 通用信息管理 | 需大量配置，上手成本高 | 开箱即用，场景化 AI 解析 |
| Keep / 健身 App | 单一场景 | 不覆盖生活物资 | 生活全域管理的起点 |

---

## 02 目标用户与核心场景

### 目标用户画像

| 属性 | 描述 |
|------|------|
| 年龄区间 | 25–45 岁，都市中青年群体 |
| 核心特征 | 有家庭或规律居家生活；工作节奏较快，生活管理时间有限；具备一定的数字化工具使用习惯 |
| 主要痛点 | 食材管理粗放、采购计划性差、依赖记忆造成浪费或缺货 |
| 使用频率预期 | 每周 2–4 次主动操作（购物后录入），每日被动接收提醒 |
| 次要用户 | 独居青年、租住人群、关注生活品质的 Z 世代 |

### 核心用户故事（User Stories）

#### 场景 A · 高效入库

**购物归来，快速录入**

用户购物回家，对着小票连拍 1–3 张，或长按麦克风说一句话，系统自动解析并更新所有物资库存。整个过程 **15 秒内完成**，无需手动选择品类或输入数字。

> 💬 语音示例："刚买了 30 个鸡蛋、两升牛奶，还有两斤猪肉"

#### 场景 B · 无感扣减

**每日自动消耗，库存实时估算**

用户无需每天手动记录消耗。系统根据历史行为建立**个人消耗模型**，每日零点自动模拟扣减，让库存数据保持动态有效。

> ⚙ 系统行为：每日 00:00 基于日均消耗率自动扣减库存

#### 场景 C · 即时校准

**发现偏差，随时修正**

用户打开冰箱发现实际剩余量与系统估算不符，长按语音说一句话，系统立即**覆盖当前库存值**并用实际消耗数据微调预测模型。

> 💬 语音示例："鸡蛋只剩 3 个了" → 库存设为 3，模型重新校准

#### 场景 D · 主动提醒

**智能预判，主动推送建议**

系统在用户**习惯购物日前 1 天**主动推送，基于消耗模型生成精准采购量建议，覆盖到下次购物日的预期消耗并保留 20% 安全余量。

> 📲 推送示例："建议明天购买 30 个鸡蛋、2L 牛奶"

---

## 03 功能需求规格

### 功能优先级总览

| 功能 ID | 功能名称 | 所属模块 | 优先级 | MVP 是否包含 |
|--------|---------|---------|-------|------------|
| F-101 | OCR 小票识别录入 | 智能录入 | P0 | ✅ 是 |
| F-102 | 长按语音多实体录入 | 智能录入 | P0 | ✅ 是 |
| F-103 | 手动数值输入兜底 | 智能录入 | P1 | ✅ 是 |
| F-201 | 每日自动衰减模型 | 库存引擎 | P0 | ✅ 是 |
| F-202 | 语义校准逻辑（增/改） | 库存引擎 | P0 | ✅ 是 |
| F-203 | 采购量预测算法 | 库存引擎 | P0 | ✅ 是 |
| F-204 | 冷启动初始化向导 | 库存引擎 | P0 | ✅ 是 |
| F-301 | 物资状态墙（环形进度） | 交互展示 | P0 | ✅ 是 |
| F-302 | AI 助理简报区 | 交互展示 | P0 | ✅ 是 |
| F-303 | 采购清单生成与导出 | 交互展示 | P1 | ✅ 是 |
| F-304 | 操作历史记录查看 | 交互展示 | P1 | ✅ 是 |
| F-401 | Push 通知（采购提醒） | 通知系统 | P1 | ✅ 是 |
| F-402 | 自定义物资品类 | 扩展功能 | P2 | ❌ 后续迭代 |
| F-403 | 多人共享家庭模式 | 扩展功能 | P2 | ❌ 后续迭代 |

---

### F-101 · OCR 小票识别录入

- 用户在 App 内点击"拍票"按钮，调用设备摄像头，支持连续拍摄 1–5 张图片
- 图像上传至后端，调用 LLM Vision 能力解析小票文本（OCR → 结构化）
- 提取字段：品类名称、数量、单位，自动映射至系统预定义的 6 类物资
- 映射失败的品类显示"未识别"并标黄，用户可手动确认或跳过
- 解析结果以"确认卡片"形式弹出，用户一键确认或单项修改后入库
- 支持图片质量检测：模糊或光线不足时提示重拍，不直接丢弃
- 异常处理：LLM 无法解析时降级为手动输入，给出友好错误提示

> **🔧 技术实现说明**  
> 优先使用 GPT-4o Vision / Claude 3.5 Sonnet 的图像理解能力，将小票图片直接输入 LLM，配合 Prompt 工程提取结构化数据（JSON），无需额外 OCR 服务，降低接入复杂度。

---

### F-102 · 长按语音多实体录入

- 首页底部常驻语音按钮，**长按即开始录音**，松开即结束，无需激活词
- 录音期间展示实时音量波形动画，给予视觉反馈
- 松开后 1.5s 内完成 STT（语音转文字）+ AI 实体解析，显示解析结果
- AI 解析能力：识别动词意图（买了/增加→累加；只剩/还有→覆盖；喝完了/用完了→清零）
- AI 解析能力：识别名词实体（鸡蛋/蛋 → 鸡蛋；牛奶/奶 → 牛奶；猪肉/肉 → 猪肉等）
- AI 解析能力：识别数量单位（个、升、L、斤、公斤、袋、盒）并统一转换
- 支持单次话术包含多实体：如"买了 30 个蛋、2L 奶，猪肉还剩半斤"
- 解析结果以列表微卡片展示，每条可单独修改/删除，整体一键确认
- 语音不清晰或环境噪声过大时，给出"未能识别，请重试"提示并保留重录选项

> **📝 Prompt 设计规范**  
> 语音解析 Prompt 须明确角色（家庭物资管理助手）、输出格式（JSON 数组，含 item / action / value / unit 字段）、边界情况处理（数量为 0 时的语义、模糊数量如"几个"的处理策略）。详见第 07 章 Prompt 模板。

---

### F-201 · 每日自动衰减模型

- 每日 **00:05**（UTC+8）触发定时任务，对所有用户的活跃物资执行自动扣减
- 扣减公式：`Remaining(t) = Last_Calibrated_Stock - (Daily_Avg_Rate × Δt_days)`
- 日均消耗率初始值由冷启动向导中用户填写的"每周消耗量"除以 7 得出
- 日均消耗率随用户校准行为动态更新（指数加权移动平均，权重 α=0.3）
- 库存计算值跌至 0 时不再继续扣减（下限锁定），避免出现负库存
- 扣减操作写入 ACTIVITY_LOG，action_type = "Auto_Decay"

```
Remaining(t) = Calibrated_Stock − Daily_Avg_Rate × Δt

Δt = 当前时间距最后一次校准时间的天数差；结果下限为 0
```

---

### F-202 · 语义校准逻辑

| 触发词类型 | 关键词示例 | 执行操作 | 更新日均值 |
|----------|----------|---------|----------|
| 覆盖型 | 只剩、还有、剩余、大概有 | current_stock = 用户指定值 | 是（用实际数据重新锚定） |
| 累加型 | 买了、购买了、增加了、又买了 | current_stock += delta | 否（仅更新库存） |
| 清零型 | 喝完了、用完了、没了、吃完了 | current_stock = 0 | 是（记录清零事件） |
| 消耗型 | 用了、吃了、用掉了 | current_stock -= delta | 是（参与消耗率计算） |

---

### F-203 · 采购预测算法

```
预测采购量 = (Daily_Avg_Rate × 7) − Current_Stock + (Daily_Avg_Rate × 7 × 20%)

即：支撑 7 天消耗所需总量 − 当前已有库存 + 20% 安全余量缓冲
```

- 预测以"距下次采购日"为基础，默认 7 天，用户可在设置中调整采购周期
- 结果向上取整至合适规格（如鸡蛋取整 10 个，牛奶取整 1L）
- Current_Stock ≥ 需求量时，建议显示"库存充足，无需采购"
- 计算结果实时展示在 AI 助理简报区，周五晚间推送为主要触达时机

---

### F-204 · 冷启动初始化向导

首次安装后用户进入 3 步初始化流程，解决数据冷启动问题：

- **步骤 1**：欢迎页 + 功能介绍，展示核心价值（一句话：帮你自动管理家里的物资）
- **步骤 2**：初始库存录入——引导用户用语音或数字输入当前各类物资的大概库存量
- **步骤 3**：消耗习惯设置——每类物资"每周大约消耗多少"，作为日均消耗率初始值
- 全程可跳过，跳过后系统使用默认值（消耗率为 0，库存为 0）并在首页引导补全
- 向导完成后不再重复出现，但可在设置中重新进入

---

## 04 数据架构设计

### 核心数据模型（完整版）

```
// ── USERS 用户表 ──────────────────────────────────
User {
  id              UUID          // 用户唯一标识
  phone           String        // 手机号（登录凭证）
  created_at      DateTime      // 注册时间
  onboarded       Boolean       // 是否完成初始化向导
  shopping_day    Int           // 习惯采购日（0=周日...6=周六）
  shopping_cycle  Int           // 采购周期（天数，默认 7）
  timezone        String        // 时区（默认 Asia/Shanghai）
}

// ── INVENTORY 物资库存表 ───────────────────────────
Inventory {
  id              UUID
  user_id         UUID          // 外键 → User
  item_key        Enum          // 枚举：EGG | MILK | MANTOU | RICE | PORK | VEG
  item_name       String        // 显示名称：鸡蛋/牛奶/馒头/大米/猪肉/蔬菜
  unit            String        // 计量单位：个/L/个/kg/g/份
  current_stock   Float         // 当前预估库存（实时计算值）
  max_stock       Float         // 满载量（用于百分比计算，用户设置）
  daily_avg_rate  Float         // 日均消耗率（动态更新）
  last_calibrated DateTime      // 最后一次人工校准时间
  last_updated    DateTime      // 最后数据变更时间（含自动衰减）
  status          Enum          // PLENTY | WARNING | CRITICAL | EMPTY
  warning_threshold Float       // 预警阈值百分比（默认 30%）
}

// ── ACTIVITY_LOG 操作日志表 ────────────────────────
ActivityLog {
  id              UUID
  user_id         UUID
  item_id         UUID          // 外键 → Inventory
  action_type     Enum          // OCR | VOICE | MANUAL | AUTO_DECAY | CALIBRATE
  operation       Enum          // ADD | SET | SUBTRACT | CLEAR
  delta_value     Float         // 变更量（正数增，负数减，SET 时为目标值）
  before_value    Float         // 操作前库存快照
  after_value     Float         // 操作后库存快照
  raw_text        String?       // 原始语音文字 / OCR 文本（调试用）
  timestamp       DateTime
  confidence      Float?        // AI 解析置信度（0-1）
}

// ── PURCHASE_SUGGESTION 采购建议表 ────────────────
PurchaseSuggestion {
  id              UUID
  user_id         UUID
  generated_at    DateTime
  items           JSON          // [{item_key, suggested_qty, unit, reason}]
  status          Enum          // PENDING | DONE | DISMISSED
  completed_at    DateTime?
}
```

### 物资品类配置（MVP 6 类）

| item_key | 显示名 | 单位 | 满载量参考 | 预警阈值 | 同义词映射 |
|---------|-------|------|----------|---------|----------|
| EGG | 🥚 鸡蛋 | 个 | 30 个 | 6 个 | 蛋、鸡蛋、eggs |
| MILK | 🥛 牛奶 | L（升） | 4L | 1L | 奶、牛奶、milk |
| MANTOU | 🍞 馒头 | 个 | 20 个 | 4 个 | 馍、馍馍、馒头 |
| RICE | 🌾 大米 | kg | 10kg | 2kg | 米、米饭、大米 |
| PORK | 🥩 猪肉 | g | 1500g | 300g | 肉、猪肉、五花肉、里脊 |
| VEG | 🥬 蔬菜 | 份 | 7 份 | 2 份 | 菜、蔬菜、青菜、绿菜 |

---

## 05 交互流程设计

### 主流程：语音录入

```
首页 → 长按语音键（开始录音） → 松开（STT 转写） → AI 解析（实体提取）
     → 确认卡片（用户确认） → 更新库存（写入数据库）

异常路径：AI 解析置信度 < 0.7 时，标注低置信字段为橙色，提示用户核实；
         解析完全失败时，降级为手动输入。
```

### 主流程：OCR 拍票识别

```
首页 → 拍票按钮（调用相机） → 上传图片（1-5 张） → LLM Vision（结构化解析）
     → 确认卡片（勾选确认） → 批量入库（库存更新）
```

### 后台自动化流程

```
定时器（每日 00:05） → 消耗计算（Δt × 日均率） → 库存扣减（写入日志）
                    → 预测计算（生成建议） → 状态判断（是否触警）
                    → Push 推送（采购提醒）
```

### 关键交互细节规范

| 交互场景 | 交互行为 | 反馈形式 | 超时处理 |
|---------|---------|---------|---------|
| 语音录入中 | 长按按钮高亮 + 波形动画 | 实时振动反馈（轻触觉） | 超过 60s 自动停止 |
| AI 解析中 | Loading 动画 + "正在理解..." | 进度文案提示 | 3s 超时显示错误提示 |
| 确认卡片展示 | 底部 Sheet 弹出，列出所有解析实体 | 每条显示品类图标+数量+单位 | 用户 30s 无操作自动关闭 |
| 库存更新成功 | Toast 提示"已更新 3 项库存" | 环形进度条动画过渡 | — |
| 库存预警 | 对应物资环形圈变红/橙 | AI 简报区显示警告文案 | — |

---

## 06 UI 设计规范

### 页面结构总览

| 页面 | 核心内容 | 导航方式 |
|------|---------|---------|
| 首页（Home） | AI 简报区、6 类物资状态墙、底部操作栏 | 底部 Tab · 首屏 |
| 采购清单（List） | AI 生成的采购建议、历史清单记录 | 底部 Tab |
| 操作历史（Log） | 所有入库/扣减/校准记录时间轴 | 底部 Tab |
| 设置（Settings） | 采购周期、物资满载量、通知时间、账号 | 底部 Tab |
| 语音录入（Modal） | 录音界面、解析结果确认卡片 | 全屏 Modal |
| OCR 录入（Modal） | 相机拍摄、上传、解析结果确认 | 全屏 Modal |
| 初始化向导（Onboarding） | 3 步引导流程 | 首次启动强制流程 |

### 首页核心组件规范（F-301 物资状态墙）

- **布局：** 2×3 网格排列，6 个物资卡片等宽等高，每卡片含环形进度条 + 品类图标 + 名称 + 当前数量
- **环形进度条：** 以满载量为 100% 基准
  - 充足（>60%）→ 绿色
  - 预警（30%–60%）→ 橙色
  - 紧缺（<30%）→ 红色
  - 告罄（=0）→ 灰色
- **数量展示：** 中心显示当前库存数值 + 单位，字号不低于 18sp
- **触摸交互：** 点击单个卡片进入物资详情页（含历史消耗折线图）
- **长按卡片：** 快捷进入该物资的语音校准模式

### 设计风格方向

| 维度 | 方向建议 |
|------|---------|
| 整体风格 | 轻量温暖的居家感，避免冷硬科技风；参考：Notion + 日系便签 App 的混合风格 |
| 主色调 | 米白/浅米色背景（#FAF8F5），搭配暖橙强调色（#C9410A），深墨色文字 |
| 字体 | 中文：思源黑体/苹方；数字/代码：等宽字体；标题区可用衬线字体增加层次感 |
| 圆角与间距 | 卡片圆角 12–16dp，元素间距遵循 8dp 基础栅格 |
| 动效 | 进度条变化用 300ms ease-out 动画；Modal 弹出用 250ms spring 动效；避免过度装饰 |
| 暗色模式 | P1 优先级，跟随系统主题，背景改为深墨色，维持色彩语义不变 |

---

## 07 算法模型详细设计

### 7.1 消耗率学习模型

系统采用指数加权移动平均（EWMA）对日均消耗率进行动态更新：

```
New_Rate = α × Actual_Rate + (1 − α) × Old_Rate

α = 0.3（平滑系数）
冷启动期（< 7 天）α 升至 0.5 以加速收敛
```

| 触发场景 | Actual_Rate 计算方式 |
|---------|-------------------|
| 用户执行"覆盖校准"（只剩 N 个） | (Last_Calibrated - N) / Δt_days |
| 用户执行"消耗记录"（用了 N 个） | N / 1（当天消耗） |
| 用户执行"清零"（喝完了） | Last_Calibrated / Δt_days |

### 7.2 AI 解析 Prompt 框架

```
// 语音解析 System Prompt 模板

你是一个家庭物资管理助手。用户会用自然语言描述物资变动情况。
请从输入中提取所有物资操作，以 JSON 数组返回，格式如下：

[{
  "item_key": "EGG|MILK|MANTOU|RICE|PORK|VEG",
  "operation": "ADD|SET|SUBTRACT|CLEAR",
  "value": 数字,
  "unit": "原始单位",
  "normalized_value": 转换后标准单位数值,
  "confidence": 0.0-1.0
}]

规则：
- "买了/又买了/增加了" → operation: ADD
- "只剩/还有/剩余" → operation: SET
- "用了/吃了/用掉" → operation: SUBTRACT
- "没了/用完了/喝完了" → operation: CLEAR, value: 0
- 斤→g(×500), 两→g(×50), 升/L→L(×1), 毫升→L(÷1000)
- 无法识别的物资：item_key 设为 "UNKNOWN"
- 数量模糊（如"几个"）：value 设为 null, confidence < 0.5
```

### 7.3 采购建议生成时机

| 触发条件 | 触发方式 | 建议内容 |
|---------|---------|---------|
| 任意物资库存跌破预警阈值 | 实时触发，首页简报区更新 | 点名预警物资 + 建议采购量 |
| 距用户习惯采购日前 1 天晚间 20:00 | 定时 Push 通知 | 完整采购清单 + 各项建议量 |
| 用户主动点击"生成采购清单" | 手动触发 | 实时计算完整清单 |

---

## 08 非功能需求

### 核心性能指标

| 指标 | 目标值 |
|------|-------|
| 语音松开后 AI 解析完成时间 | ≤ 1.5s（P99） |
| 6 类核心物资语音实体识别准确率 | ≥ 95% |
| OCR 小票解析完成时间（单张） | ≤ 3s |

### 完整非功能需求

| 维度 | 指标 | 说明 |
|------|------|------|
| 可用性 | 99.5% SLA（月度） | 核心功能每月不可用时间 < 3.6 小时 |
| 并发 | MVP 阶段支持 1000 DAU | 初期服务器配置按此规格，后续弹性扩展 |
| 数据安全 | 传输 TLS 1.3，存储 AES-256 | 用户行为数据本地优先，敏感数据脱敏后上云 |
| 离线能力 | 本地库存可离线查看 | 断网时展示最后同步数据，录入操作排队上传 |
| 包体大小 | iOS/Android 安装包 < 40MB | 图片资源懒加载，AI 能力云端调用不打包模型 |
| 兼容性 | iOS 15+ / Android 10+ | 覆盖近 3 年主流设备 |
| 隐私合规 | 符合《个人信息保护法》 | 用户协议明确数据使用范围，提供数据导出与删除功能 |
| 可扩展性 | 模块化架构 | 物资管理模块完全解耦；新模块通过插件方式接入 |

---

## 09 技术选型建议

> **📌 说明：** 以下为基于 MVP 快速交付原则的建议选型，最终技术决策需结合团队技术栈与资源状况确定。

| 层级 | 技术方向 | 推荐选型 | 选择理由 |
|------|---------|---------|---------|
| 移动端 App | 跨平台框架 | React Native / Flutter | 一套代码覆盖 iOS + Android，MVP 阶段降低成本 |
| 后端服务 | API 服务 | Node.js (Fastify) / Python (FastAPI) | 轻量快速，与 AI API 对接友好 |
| 数据库 | 主数据库 | PostgreSQL | 支持 JSON 字段，事务可靠，免费开源 |
| AI 能力 | LLM 调用 | Claude Sonnet / GPT-4o | 语音实体解析 + OCR 识别，强大的中文理解能力 |
| 语音转文字 | STT 服务 | 讯飞语音 / Whisper API | 讯飞中文识别准确率高；Whisper 作为备选 |
| 推送通知 | Push 服务 | Firebase Cloud Messaging (FCM) | 统一 iOS/Android 推送，免费额度充足 |
| 定时任务 | Cron 调度 | Bull Queue + Redis | 可靠的后台任务队列，支持失败重试 |
| 云服务 | 部署环境 | 阿里云 / 腾讯云 | 国内合规，备案便利，CDN 覆盖好 |
| 监控 | 日志与告警 | Sentry + Grafana | 前端错误追踪 + 后端性能监控 |

### MVP 开发优先级与估时（参考）

| Sprint | 主要任务 | 估算周期 |
|--------|---------|---------|
| Sprint 1 | 项目初始化、数据库设计、用户注册/登录、初始化向导 | 1 周 |
| Sprint 2 | 首页物资状态墙、手动录入、自动衰减后端逻辑 | 1 周 |
| Sprint 3 | 语音录入（STT + AI 解析 + 确认卡片） | 1.5 周 |
| Sprint 4 | OCR 小票识别、采购预测算法、AI 简报区 | 1.5 周 |
| Sprint 5 | Push 通知、采购清单导出、操作历史页 | 1 周 |
| Sprint 6 | 测试、Bug 修复、性能优化、内测发布准备 | 1 周 |

---

## 10 开发计划与工程治理（v1.1 新增）

### 10.1 工程治理前置条件（开发开始前必须完成）

- 在功能开发启动前，必须先完成模块边界定义与目录初始化，不允许先写业务代码后补结构。
- 每个业务模块必须独立存放目录，禁止跨模块直接读写对方内部实现。
- 模块间协作只能通过公开接口（service / API contract / facade），不允许跨模块调用私有文件。
- 架构评审通过后方可进入编码阶段；未经评审通过的模块设计不得立项开发。

### 10.2 模块化目录规范（强制）

推荐目录骨架如下（可按技术栈映射，但模块边界不变）：

```text
src/
  core/                    # 通用基础能力（日志、网络、鉴权中间件、基础组件）
  modules/
    auth/
      api/
      domain/
      data/
      ui/
      tests/
    inventory/
      api/
      domain/
      data/
      ui/
      tests/
    voice/
      api/
      domain/
      data/
      ui/
      tests/
    ocr/
      api/
      domain/
      data/
      ui/
      tests/
    push/
      api/
      domain/
      data/
      ui/
      tests/
    admin/
      api/
      domain/
      data/
      ui/
      tests/
```

目录规范要求：

- `core/` 仅承载跨模块复用能力，不承载业务流程逻辑。
- `modules/*` 目录必须按业务域划分，所有新增功能必须归属唯一模块。
- 每个模块至少包含：`api`、`domain`、`data`、`ui`、`tests` 五类子目录。
- 新增模块时须先提交模块设计说明（职责、上下游依赖、对外接口）。

### 10.3 代码文件规模约束（强制）

- 单个手写代码文件行数上限为 `800` 行（含注释与空行）。
- 任一文件超过 800 行，必须按职责进行拆分，不允许使用注释折叠规避拆分。
- 适用范围：客户端代码、服务端代码、测试代码、脚本代码。
- 例外范围：自动生成代码（如 `*.g.dart`、`*.pb.*`、OpenAPI 生成文件）可超限，但必须标记为 generated 且禁止手改。

### 10.4 模块 DoD（Definition of Done）

每个模块合并前必须同时满足：

- 目录合规：模块目录结构满足 10.2 要求。
- 边界合规：无跨模块私有实现调用。
- 规模合规：手写代码文件无超过 800 行。
- 质量合规：单元测试覆盖达标（核心业务逻辑覆盖率不低于 80%）。
- 文档合规：对外接口与变更说明同步更新到 PRD/API 文档。

### 10.5 评审与 CI 门禁（强制阻断）

PR 与发布流水线新增以下必过项：

- `line-limit-check`：扫描手写代码文件，超过 800 行直接阻断合并。
- `module-boundary-check`：检测跨模块依赖规则，违反白名单直接阻断。
- `architecture-review-check`：高风险变更需附架构评审记录（模块边界与拆分依据）。

人工评审要求：

- Code Review 需显式检查文件拆分合理性与模块边界。
- 架构评审单需记录依赖方向、公开接口、异常处理策略、回滚策略。

### 10.6 工程级接口规范（Public Interfaces / Types）

| 规范项 | 规则 |
|------|------|
| Module Public API | 每个模块仅允许通过 `index`/`facade` 暴露对外能力，禁止直接暴露内部实现文件 |
| Dependency Rules | 模块依赖方向固定，禁止循环依赖，禁止跨层逆向调用 |
| Generated Code Policy | 自动生成代码集中归档到 generated 目录或后缀规范路径，必须可追溯来源并禁止手改 |

### 10.7 规范验收与测试计划

规范验收：

- 任一新功能提交时，必须映射到唯一模块目录。
- 代码扫描报告中手写代码文件不得超过 800 行。

CI 验收：

- `line-limit-check` 与 `module-boundary-check` 必须通过后才允许合并。

评审验收：

- 架构评审单必须记录模块边界定义与拆分依据，作为发布前准入条件。

### 10.8 默认假设与生效策略

- “单文件不超过 800 行”默认适用于所有手写业务与测试代码。
- 自动生成代码不纳入 800 行硬限制，但需在仓库中明确 generated 来源并禁止手动修改。
- 该规范自 `v1.1` 起生效；存量代码按“增量触达即整改”原则逐步收敛。

---

## 11 API 接口契约与鉴权规范（v1.1 新增）

### 11.1 接口全局约定

- 基础路径：`/api/v1`（主版本通过路径管理）。
- 协议：HTTPS + TLS 1.3。
- 字段命名：`snake_case`；时间格式统一 ISO 8601（UTC 存储，客户端按用户时区展示）。
- 写接口必须支持幂等键：`Idempotency-Key`。
- 分页参数统一：`page`、`page_size`、`sort_by`、`sort_order`。
- 请求头统一：
  - `Authorization: Bearer <access_token>`
  - `X-Request-Id: <uuid>`
  - `X-App-Version: <semver>`
  - `X-Platform: ios|android|web-admin`

### 11.2 统一响应模型

成功响应：

```json
{
  "code": "OK",
  "message": "success",
  "request_id": "4f9a2f8f-9218-4e15-ae11-3a3c417bb6d1",
  "timestamp": "2026-03-31T08:00:00Z",
  "data": {}
}
```

失败响应：

```json
{
  "code": "AUTH_401_TOKEN_EXPIRED",
  "message": "access token expired",
  "request_id": "a0ad2f18-9d8d-4b15-80bf-e04f2a108889",
  "timestamp": "2026-03-31T08:00:00Z",
  "details": {"expired_at": "2026-03-31T07:59:59Z"},
  "retriable": true
}
```

### 11.3 错误码与错误信息规范

| 分类 | 错误码 | 含义 | 客户端处理 |
|------|------|------|----------|
| 鉴权 | AUTH_401_UNAUTHORIZED | 未登录或签名非法 | 跳登录页，清理本地会话 |
| 鉴权 | AUTH_401_TOKEN_EXPIRED | Access Token 过期 | 自动刷新，失败则重登 |
| 鉴权 | AUTH_403_FORBIDDEN | 角色权限不足 | 展示无权限页 |
| 鉴权 | AUTH_409_SESSION_REVOKED | 会话被强制下线 | Toast 提示并回到登录 |
| 参数 | VAL_400_INVALID_PARAM | 参数格式错误 | 表单高亮错误字段 |
| 业务 | BIZ_404_ITEM_NOT_FOUND | 目标物资不存在 | 刷新列表并提示 |
| 业务 | BIZ_409_CONFLICT | 数据版本冲突 | 拉取最新快照并提示重试 |
| AI 服务 | AI_429_RATE_LIMIT | AI 解析限流 | 显示降级入口（手动录入） |
| AI 服务 | AI_504_TIMEOUT | AI 解析超时 | 提示重试，保留原输入 |
| 系统 | SYS_500_INTERNAL | 服务内部错误 | 友好提示，埋点上报 |
| 系统 | SYS_503_DEPENDENCY_DOWN | 第三方依赖不可用 | 触发降级策略 |

### 11.4 鉴权与会话策略

- 登录方式：手机号 + 验证码。
- Token 体系：
  - Access Token：JWT（RS256），有效期 2 小时。
  - Refresh Token：随机字符串 + 设备绑定，有效期 30 天，单次刷新后轮换。
- JWT Payload 最小字段：`sub`（用户 ID）、`sid`（会话 ID）、`scope`、`iat`、`exp`、`device_id`。
- 刷新机制：
  - Access Token 过期时客户端静默刷新一次。
  - 刷新失败或 Refresh Token 失效时，强制重登。
- 多端策略（默认）：
  - 同时在线设备上限 3 台。
  - 第 4 台登录时踢出最早活跃设备，并写入安全日志。
- 注销账号：
  - 发起后进入 7 天冷静期，可撤销。
  - 冷静期结束后执行软删除 + 30 天后硬删除。

### 11.5 核心接口契约（工程可开发级）

#### 11.5.1 认证接口

| 接口 | Method | Request | Response |
|------|--------|---------|----------|
| `/auth/sms/send` | POST | `phone:string(必填)` `purpose:LOGIN|DELETE_ACCOUNT` | `expire_in:int` `retry_after:int` |
| `/auth/sms/login` | POST | `phone:string` `code:string` `device_id:string` | `access_token` `refresh_token` `user_profile` |
| `/auth/token/refresh` | POST | `refresh_token:string` `device_id:string` | `access_token` `refresh_token` |
| `/auth/logout` | POST | `session_id:string` | `success:boolean` |
| `/auth/account` | DELETE | `verify_code:string` `reason:string?` | `cool_down_end_at:datetime` |

#### 11.5.2 库存与操作接口

| 接口 | Method | Request | Response |
|------|--------|---------|----------|
| `/inventory/items` | GET | `status?:PLENTY|WARNING|CRITICAL|EMPTY` | `items:[InventoryItem]` |
| `/inventory/operations/batch` | POST | `operations:[{item_key,operation,value,unit,source}]` | `updated_items` `activity_ids` |
| `/inventory/logs` | GET | `page` `page_size` `item_key?` | `list:[ActivityLog]` `total:int` |
| `/inventory/items/{item_key}/calibrate` | POST | `value:float` `unit:string` | `item:InventoryItem` |

#### 11.5.3 AI 解析接口

| 接口 | Method | Request | Response |
|------|--------|---------|----------|
| `/ai/voice/parse` | POST | `audio_url:string` `locale:string` | `entities:[ParsedEntity]` `confidence:float` |
| `/ai/ocr/parse` | POST | `image_urls:[string]` | `entities:[ParsedEntity]` `unknown_items:[string]` |
| `/ai/parse/confirm` | POST | `session_id:string` `entities:[ParsedEntity]` | `inventory_snapshot` |

#### 11.5.4 采购建议与推送接口

| 接口 | Method | Request | Response |
|------|--------|---------|----------|
| `/suggestions/generate` | POST | `mode:AUTO|MANUAL` | `suggestion_id` `items` |
| `/suggestions/latest` | GET | - | `suggestion` |
| `/push/device/register` | POST | `platform` `device_token` | `registered:boolean` |
| `/push/preference` | PUT | `enabled:boolean` `notify_time:string` | `preference` |

### 11.6 离线同步契约与冲突策略

- 本地缓存范围：
  - 必存：`inventory_snapshot`（TTL 24h）、`latest_suggestion`（TTL 24h）、`settings`（TTL 7d）。
  - 可选：`activity_log_recent`（最近 200 条）。
- 离线队列：
  - 写操作统一入 `offline_ops_queue`，字段：`op_id`、`created_at`、`payload`、`retry_count`。
  - 网络恢复后按时间顺序重放，单条最多重试 5 次（指数退避）。
- 冲突解决：
  - 以服务端版本为基准。
  - 客户端按 `op_id` 幂等重放未落库操作。
  - 冲突返回 `BIZ_409_CONFLICT` 时先拉最新快照，再自动重放一次。

### 11.7 接口版本与废弃策略

- 大版本：路径版本（`/api/v1`、`/api/v2`）。
- 小版本：Header `X-API-Revision`（向后兼容变更）。
- 废弃策略：
  - `Deprecated` 响应头提前 90 天公告。
  - 保持双栈窗口期 60 天。
  - 窗口期结束后返回 `API_410_GONE`。

---

## 12 iOS 页面交互规格（v1.1 新增）

### 12.1 设计方向（卡片式流式简约）

- 布局原则：卡片流式分区 + 大留白 + 低信息噪声。
- 视觉原则：浅底色、深文字、暖色点缀；强调“可读优先，不堆特效”。
- 交互原则：一次只做一件事，主路径 1-2 步可达。

### 12.2 iOS 设计 Token（执行级）

| 类别 | 规范 |
|------|------|
| 背景色 | `#F7F6F2`（主背景）、`#FFFFFF`（卡片） |
| 主文字 | `#1F1F1F` |
| 辅助文字 | `#6F6F6F` |
| 强调色 | `#E5BE45`（强调）、`#C9410A`（风险警示） |
| 圆角 | 卡片 `16`，底部 Sheet `20` |
| 阴影 | 低强度阴影：y=4 blur=16 opacity=8% |
| 间距 | 8pt 栅格，常用 8/12/16/24 |
| 字体 | SF Pro + 苹方，数字优先使用等宽数字 |
| 动效 | 200-300ms，`easeOut`；关键操作反馈 < 100ms |

### 12.3 全局导航与返回规则

- 导航结构：底部 Tab（Home/List/Log/Settings）+ 全屏 Modal（Voice/OCR）。
- 返回规则：
  - Tab 内层页面使用导航栈返回。
  - Modal 下拉关闭前需判断是否有未提交变更，未提交需二次确认。
- 深链规则：
  - 支持跳转到指定物资详情与指定采购建议。
  - 深链打开时若未登录，先到登录页，登录成功后回跳原目标页。

### 12.4 页面规格：Home

- 元素清单：
  - 顶部问候区、AI 简报卡、6 宫格库存卡、底部操作条（语音/拍票/手动）。
- 状态定义：
  - 默认：展示最近同步库存。
  - 加载：骨架屏 + 卡片占位。
  - 成功：卡片数值与环形进度更新动画。
  - 失败：顶部错误条 + 重试按钮。
  - 空状态：未初始化时展示引导卡片。
  - 边界：库存=0 显示告罄样式并给出“去补货”入口。
- 跳转规则：
  - 点库存卡 -> 物资详情页（参数：`item_key`）。
  - 点 AI 简报 -> 采购清单页（参数：`suggestion_id`）。
- 键盘/滚动/手势：
  - Home 为纵向滚动，顶部信息吸附。
  - 长按库存卡可直接进入语音校准 Modal。

### 12.5 页面规格：采购清单（List）

- 元素清单：建议头卡、清单条目卡、状态筛选器、导出按钮。
- 状态定义：默认/加载/生成中/失败/空历史。
- 跳转规则：
  - 条目点击 -> 物资详情页（`item_key`）。
  - 导出 -> 系统分享面板（文本/PDF）。
- 键盘/滚动/手势：
  - 支持下拉刷新。
  - 筛选弹层使用底部 Sheet，支持下滑关闭。

### 12.6 页面规格：操作历史（Log）

- 元素清单：时间轴列表、筛选区、搜索框、详情抽屉。
- 状态定义：默认/加载/失败/空状态/离线只读。
- 跳转规则：
  - 点击记录 -> 操作详情抽屉（不离开当前页）。
- 键盘/滚动/手势：
  - 搜索框聚焦时列表自动上移避让键盘。
  - 顶部筛选区滚动吸顶。

### 12.7 页面规格：设置（Settings）

- 元素清单：采购周期、满载量设置、通知时段、账号与隐私、重新初始化入口。
- 状态定义：默认/保存中/保存成功/保存失败。
- 跳转规则：
  - 账号与安全 -> 登录设备页、注销页。
  - 隐私与协议 -> WebView 文档页。
- 键盘/滚动/手势：
  - 数值输入必须键盘过滤（仅数字/小数）。
  - 保存按钮固定底部，不随滚动离场。

### 12.8 页面规格：语音录入 Modal

- 元素清单：录音主按钮、实时波形、识别文本、实体卡片列表、确认按钮。
- 状态定义：
  - 默认、录音中、解析中、解析成功、解析失败、低置信提示。
- 跳转规则：
  - 确认提交 -> 回 Home 并刷新卡片。
  - 取消 -> 返回来源页。
- 键盘/滚动/手势：
  - 录音中禁用右滑返回。
  - 支持下拉关闭（仅在非录音状态）。

### 12.9 页面规格：OCR 录入 Modal

- 元素清单：拍照入口、图片列表、上传进度、解析结果卡、逐项编辑入口。
- 状态定义：默认/拍摄中/上传中/解析中/解析失败/部分未识别。
- 跳转规则：
  - 部分未识别 -> 进入手动补全页（带 `unknown_items` 参数）。
- 键盘/滚动/手势：
  - 编辑数量时自动弹数字键盘。
  - 图片横向滑动浏览，纵向滑动关闭仅在顶部触发。

### 12.10 页面规格：Onboarding + Auth

- Onboarding：
  - 三步流程：欢迎 -> 初始库存 -> 消耗习惯。
  - 每步含“跳过”与“下一步”；跳过需二次确认。
- Auth：
  - 登录页元素：手机号输入、验证码输入、发送按钮、隐私协议勾选。
  - 状态：发送中、倒计时、验证失败、风控限制。
  - 校验：手机号格式、验证码长度、频控提示（秒级倒计时）。

---

## 13 管理后台 UI/UX 设计规范（v1.1 新增）

### 13.1 视觉基线（参考图风格）

- 风格关键词：浅色仪表盘、模块化大卡片、圆角高统一、低饱和配色、信息清晰分区。
- 页面结构：顶栏导航 + 左侧功能区（可折叠）+ 主内容卡片流。
- 视觉层级：
  - L1：关键 KPI（大数字卡）
  - L2：趋势图与任务卡
  - L3：列表表格与明细抽屉

### 13.2 后台设计 Token

| 类别 | 规范 |
|------|------|
| 页面背景 | `#ECEEF2` |
| 内容底板 | `#F8F7F2` |
| 卡片背景 | `#FFFFFF` |
| 主文字 | `#1D1D1D` |
| 强调色 | `#E7C84F` |
| 警告色 | `#E27D3C` |
| 圆角 | 容器 `24`，卡片 `18`，按钮 `12` |
| 组件间距 | 12/16/20 |

### 13.3 信息架构与页面清单

| 菜单 | 页面目标 | 核心组件 |
|------|---------|----------|
| Dashboard | 运营总览 | KPI 卡、趋势图、告警卡、待办卡 |
| 用户管理 | 用户查询与状态治理 | 用户表格、筛选器、会话面板 |
| 内容审计 | AI 解析质量与异常处置 | 审计队列、详情卡、处置记录 |
| 推送管理 | 运营消息编排与发送 | 模板库、受众筛选、发送计划 |
| 配置中心 | 业务参数与阈值配置 | 配置表单、版本记录、回滚 |
| AB 开关 | 实验策略管理 | 开关矩阵、流量分配、实验报表 |
| 审计日志 | 操作可追溯 | 时间轴表格、操作详情抽屉 |

### 13.4 后台页面级交互规格（执行级）

- Dashboard：
  - 默认展示今日核心指标、近 7 天趋势、高优先级告警。
  - 支持时间范围切换（今天/7 天/30 天）与卡片拖拽排序。
- 用户管理：
  - 支持手机号、用户 ID、状态筛选。
  - 支持会话踢下线、账号冻结、注销申请查询。
- 内容审计：
  - 对低置信解析结果进行人工复核与标签修正。
  - 修正结果可回灌训练样本池。
- 推送管理：
  - 支持模板变量预览、分组灰度、定时发送、失败重试。
- 配置中心：
  - 配置变更必须走审批流；发布前显示 diff。
  - 支持一键回滚至最近稳定版本。
- AB 开关：
  - 支持按用户分层/渠道/版本灰度。
  - 必须提供紧急总开关。

### 13.5 角色权限矩阵（RBAC）

| 操作项 | 超级管理员 | 运营 | 客服 |
|------|------------|------|------|
| 查看 Dashboard | ✅ | ✅ | ✅ |
| 修改系统配置 | ✅ | ✅（需审批） | ❌ |
| 发布推送活动 | ✅ | ✅ | ❌ |
| 冻结/解冻用户 | ✅ | ❌ | ✅（仅工单场景） |
| 执行全局回滚 | ✅ | ❌ | ❌ |
| 查看审计日志 | ✅ | ✅ | ✅（脱敏） |

### 13.6 组件状态与异常处理规范

- 卡片组件必须具备：默认/加载/空/错误四态。
- 表格组件必须具备：批量选择、空结果引导、失败重试、导出状态提示。
- 网络异常时保持页面骨架不抖动，错误提示统一展示在页面右上角通知区。
- 高危操作（推送全量、配置发布、全局回滚）必须二次确认并记录审计日志。

---

## 14 测试与验收基线（v1.1 新增）

### 14.1 核心功能验收标准（AC）

| 功能 | 验收标准 |
|------|---------|
| 语音录入 F-102 | 语音结束后 1.5s 内返回解析结果；可编辑后成功入库 |
| OCR 录入 F-101 | 单张小票 3s 内返回结构化结果；未识别项可手动补全 |
| 自动衰减 F-201 | 每日任务稳定执行，库存不出现负值 |
| 采购建议 F-203 | 低库存触发建议，建议量符合公式与安全余量逻辑 |
| 离线录入 | 断网可写入队列，恢复网络后自动同步成功 |
| 鉴权刷新 | Access 过期可静默刷新，刷新失败回登录页 |

### 14.2 边界值与异常测试清单

- 数值边界：`0`、超大值、负数、浮点精度（如 0.1L）。
- 输入边界：模糊语义（“几个”“一点点”）、多实体混合单位、方言词。
- 图片边界：超大图片、模糊图片、逆光图片、连续 5 张上传。
- 会话边界：Token 过期、刷新失效、多端挤下线、权限撤销。
- 网络边界：超时、断网重连、弱网抖动、服务端 5xx、429 限流。

### 14.3 AI 解析测试集基线（至少 50 条）

- 样本分桶要求（每桶至少 5 条）：
  - 标准普通话
  - 方言口音
  - 噪声环境
  - 多实体复合表达
  - 单位换算场景
  - 意图混合（ADD + SET）
  - 模糊数量表达
  - 清零与负向表达
  - OCR 模糊文本
  - OCR 非标准票据
- 每条样本需标注：原始输入、期望解析 JSON、允许误差、置信度门槛。

### 14.4 性能测试基线

| 指标 | 阈值 |
|------|------|
| 语音解析接口 P95 | <= 1200ms |
| OCR 解析接口 P95 | <= 2800ms |
| 首页首屏可交互时间 | <= 1500ms |
| 离线队列 100 条重放耗时 | <= 30s |
| 后台 Dashboard 首屏加载 | <= 2000ms |

### 14.5 上线门禁与签署

- 必须通过：功能测试、回归测试、性能测试、灰度验证、合规检查。
- 发布前签署人：产品负责人、客户端负责人、服务端负责人、测试负责人、运营负责人。
- P0/P1 缺陷未清零不得上线；P2 缺陷需评估并给出延期修复计划。

---

## 15 风险评估与应对策略

### 🔴 高风险

**风险①：AI 解析准确率不达标**

- **描述：** 语音输入方言、噪声、特殊表达导致识别率低于 95% 目标，影响用户信任。
- **应对：** 构建包含 200+ 测试用例的解析测试集，灰度期持续监控低置信度记录；提供明确的"修改"入口，确保错误可纠正；收集用户纠正数据持续优化 Prompt。

**风险②：用户录入习惯难以养成**

- **描述：** MVP 核心假设是用户会主动录入，但行为习惯养成是最难突破的壁垒。
- **应对：** 极度降低录入摩擦（一键语音）；首月发送购物提醒，引导录入；在推送文案中突出"未录入=预测不准"的后果，正向激励用户。

### 🟡 中风险

**风险③：LLM 调用成本随用户增长**

- **描述：** 每次语音/OCR 解析均需调用 LLM API，成本与 MAU 正相关，用户量增长后边际成本压力大。
- **应对：** MVP 阶段按 Token 精算，设置每日调用上限；商业化后通过会员订阅分摊 AI 成本；长期探索轻量本地模型替代部分解析场景。

**风险④：消耗模型冷启动数据不足**

- **描述：** 新用户前 1–2 周数据稀少，消耗模型预测偏差大，影响采购建议质量。
- **应对：** 冷启动向导收集用户主动填写的消耗习惯作为初始值；向导界面提供"参考建议值"；前 14 天在建议文案中注明"建议仅供参考，数据还在积累中"。

### 🟢 低风险

**风险⑤：应用审核问题**

- **描述：** iOS App Store 对麦克风/相机权限要求严格的隐私说明，审核可能延迟。
- **应对：** 提前准备完善的隐私政策与权限使用说明；权限申请时机合理（首次使用语音功能时申请麦克风权限，而非安装即申请）。

---

## 16 版本迭代路线图

### 产品路线图

| 版本 | 阶段 | 核心功能 |
|------|------|---------|
| **v1.0 MVP**（当前） | 生活物资智能管理 | 语音多实体录入、OCR 小票识别、自动衰减模型、采购预测、状态墙展示、Push 提醒 |
| **v1.5** | 物资模块深化 | 自定义品类、家庭成员协作、月度消费报告、暗色模式、Widget 小组件 |
| **v2.0** | 工作与日程模块 | 任务优先级 AI 排序、日历整合、会议智能摘要、工作周报生成 |
| **v3.0** | 社交与关系模块 | 重要人物纪念日提醒、联系频率管理、礼物建议、家庭事务协同 |
| **v4.0** | 职业规划模块 | 技能成长追踪、职业路径规划、简历助手、行业资讯订阅 |

### 商业化路径规划

| 阶段 | 商业模式 | 核心策略 |
|------|---------|---------|
| MVP 期（v1.0） | 完全免费 | 积累种子用户，收集真实使用数据，打磨核心体验 |
| 成长期（v1.5） | Freemium | 免费版：单人物资管理；Pro 版（¥18/月）：家庭共享 + 高级分析 + 无限品类 |
| 扩张期（v2.0+） | 订阅制 | 全模块订阅（¥38/月 或 ¥298/年）；企业/团队版探索 |
| 成熟期 | 多元变现 | API 开放平台；品牌采购合作（植入推荐）；数据洞察服务（匿名化） |

---

*LifeOS Assistant · PRD v1.1 · 生活物资管理模块 · MVP 阶段*  
*文档状态：待评审*
