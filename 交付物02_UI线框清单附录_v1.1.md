# SuperAssistant · 交付物 02
## UI 线框清单附录 v1.1（iOS + 管理后台）

> 适用文档：`SuperAssistant_PRD_v1.1.md` 第 12-14 章  
> 目标：用于设计评审、开发排期、联调验收的逐页清单

---

## 1. 交付边界与输出物

### 1.1 本附录覆盖范围

- iOS 端：Home、List、Log、Settings、Voice Modal、OCR Modal、Onboarding、Auth、Item Detail、Manual Fill。
- 管理后台：Dashboard、用户管理、内容审计、推送管理、配置中心、AB 开关、审计日志。

### 1.2 设计交付文件清单（必须）

| 文件类型 | 命名建议 | 说明 |
|---|---|---|
| 页面线框图 | `ios_home_wire_v1` | 低保真结构与布局 |
| 高保真视觉稿 | `ios_home_ui_v1` | Token 与组件样式落地 |
| 组件状态图 | `comp_inventory_card_states_v1` | 默认/加载/空/错误/禁用 |
| 交互流程图 | `flow_voice_input_v1` | 操作路径与异常分支 |
| 标注稿 | `ios_home_spec_v1` | 尺寸、间距、字体、颜色、动效 |
| 切图/图标表 | `asset_manifest_v1` | 命名、分辨率、用途 |

---

## 2. iOS 端线框清单（卡片式流式简约）

## 2.1 iOS 全局规范清单

| 校验项 | 标准 |
|---|---|
| 页面主背景 | `#F7F6F2` |
| 卡片背景 | `#FFFFFF` |
| 卡片圆角 | 16 |
| Sheet 圆角 | 20 |
| 间距栅格 | 8pt |
| 动效时长 | 200-300ms |
| 主路径点击层级 | 不超过 2 层 |
| 触控最小热区 | 44x44pt |

## 2.2 页面 ID 与路由参数

| 页面ID | 路由 | 入参 | 返回规则 |
|---|---|---|---|
| IOS-HOME | `/home` | - | Tab 切换 |
| IOS-LIST | `/list` | `suggestion_id?` | Tab 切换 |
| IOS-LOG | `/log` | `item_key?` | Tab 切换 |
| IOS-SETTINGS | `/settings` | - | Tab 切换 |
| IOS-VOICE | `/voice-modal` | `source_page` | 关闭回来源页 |
| IOS-OCR | `/ocr-modal` | `source_page` | 关闭回来源页 |
| IOS-ONBOARD | `/onboarding` | - | 完成后进 Home |
| IOS-AUTH | `/auth` | `redirect?` | 成功后回跳 |
| IOS-ITEM-DETAIL | `/item/{item_key}` | `item_key` | 左上返回 |
| IOS-MANUAL-FILL | `/manual-fill` | `unknown_items[]` | 提交后回 OCR |

## 2.3 IOS-HOME 线框核对

### 布局区块（从上到下）
- 顶部问候区：标题、日期、小状态标签。
- AI 简报卡：风险提示、建议摘要、跳转按钮。
- 6 宫格库存卡：`InventoryCard` x6。
- 底部悬浮操作条：语音、拍票、手动按钮。

### 组件清单
- `GreetingHeader`
- `BriefingCard`
- `InventoryCard`
- `ActionDock`
- `TopErrorBanner`

### 状态清单
- 默认态、加载骨架态、空态、失败态、离线只读态、库存为0边界态。

### 交互清单
- 点击库存卡 -> `IOS-ITEM-DETAIL`（带 `item_key`）。
- 长按库存卡 -> `IOS-VOICE`（校准模式）。
- 点击 AI 简报 -> `IOS-LIST`（带 `suggestion_id`）。

## 2.4 IOS-LIST 线框核对

### 布局区块
- 建议摘要头卡。
- 筛选栏（状态、日期）。
- 清单条目卡列表。
- 导出按钮。

### 状态清单
- 默认、生成中、空历史、失败、离线缓存态。

### 交互清单
- 下拉刷新。
- 筛选底部 Sheet。
- 导出到系统分享面板。

## 2.5 IOS-LOG 线框核对

### 布局区块
- 顶部搜索输入。
- 筛选 Chips。
- 时间轴日志列表。
- 详情抽屉。

### 状态清单
- 默认、加载、空、失败、离线只读。

### 交互清单
- 点击记录 -> 抽屉展开。
- 搜索聚焦 -> 键盘弹起 + 列表自动避让。

## 2.6 IOS-SETTINGS 线框核对

### 布局区块
- 采购周期配置卡。
- 满载量配置卡。
- 推送偏好卡。
- 账号与安全卡。
- 隐私与协议卡。
- 固定底部保存按钮。

### 状态清单
- 默认、保存中、成功提示、失败提示。

### 交互清单
- 数值输入仅数字键盘。
- 未保存离开需二次确认。

## 2.7 IOS-VOICE 线框核对

### 布局区块
- 中央录音按钮。
- 实时波形区域。
- 识别文本预览。
- 解析结果实体卡片列表。
- 底部确认按钮。

### 状态清单
- 默认、录音中、解析中、解析成功、解析失败、低置信标记态。

### 交互清单
- 录音中禁用手势返回。
- 非录音态支持下拉关闭。
- 编辑实体后提交，返回来源页并刷新。

## 2.8 IOS-OCR 线框核对

### 布局区块
- 拍照入口。
- 图片缩略图队列。
- 上传进度条。
- 解析结果卡列表。
- 未识别项入口。

### 状态清单
- 默认、拍摄中、上传中、解析中、失败、部分未识别。

### 交互清单
- 未识别项进入 `IOS-MANUAL-FILL`。
- 图片横向滑动浏览。

## 2.9 IOS-ONBOARD 与 IOS-AUTH 线框核对

### Onboarding
- 三步卡片结构：欢迎、初始库存、消耗习惯。
- 每步按钮：`跳过` + `下一步`。
- 跳过需确认弹窗。

### Auth
- 手机号输入。
- 验证码输入与倒计时按钮。
- 隐私协议勾选。
- 登录主按钮。

### 状态清单
- 发送中、倒计时中、验证码错误、风控限制。

## 2.10 iOS 页面验收勾选表

| 页面ID | 结构线框 | 高保真 | 状态图 | 跳转图 | 交互标注 | 开发对齐 |
|---|---|---|---|---|---|---|
| IOS-HOME | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-LIST | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-LOG | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-SETTINGS | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-VOICE | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-OCR | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-ONBOARD | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-AUTH | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-ITEM-DETAIL | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| IOS-MANUAL-FILL | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |

---

## 3. 管理后台线框清单（参考图风格）

## 3.1 后台全局规范清单

| 校验项 | 标准 |
|---|---|
| 页面背景 | `#ECEEF2` |
| 内容底板 | `#F8F7F2` |
| 卡片圆角 | 18 |
| 主容器圆角 | 24 |
| KPI 字号层级 | 32/24/16 |
| 组件间距 | 12/16/20 |
| 全局告警入口 | 右上通知区固定 |
| 高危操作 | 二次确认 + 审计记录 |

## 3.2 页面 ID 与功能映射

| 页面ID | 页面名 | 目标 |
|---|---|---|
| ADM-DASH | Dashboard | 核心运营总览 |
| ADM-USERS | 用户管理 | 用户状态与会话治理 |
| ADM-AUDIT | 内容审计 | 低置信结果人工复核 |
| ADM-PUSH | 推送管理 | 活动编排与发送 |
| ADM-CONFIG | 配置中心 | 参数与阈值管理 |
| ADM-AB | AB 开关 | 实验策略控制 |
| ADM-LOG | 审计日志 | 全链路可追溯 |

## 3.3 ADM-DASH 线框核对

### 布局区块
- 顶部 KPI 大卡区。
- 中部趋势图卡 + 告警卡。
- 底部任务与待办卡。

### 状态清单
- 默认、加载骨架、无数据、错误重试。

### 交互清单
- 时间维度切换（今天/7天/30天）。
- 卡片排序拖拽。

## 3.4 ADM-USERS 线框核对

### 布局区块
- 筛选条（手机号、用户 ID、状态）。
- 用户列表表格。
- 右侧详情抽屉。

### 交互清单
- 查询、重置筛选。
- 冻结/解冻、踢下线（按角色权限）。

### 状态清单
- 默认、加载、空结果、失败。

## 3.5 ADM-AUDIT 线框核对

### 布局区块
- 审计任务队列列表。
- 详情面板（原始输入、解析结果、修正区）。
- 处理历史时间轴。

### 交互清单
- 通过、驳回、修正提交。
- 回灌训练样本开关。

## 3.6 ADM-PUSH 线框核对

### 布局区块
- 模板列表。
- 受众筛选卡。
- 发送设置卡（即时/定时/灰度）。
- 发送结果面板。

### 状态清单
- 草稿、排队、发送中、完成、失败重试。

### 交互清单
- 模板预览。
- 一键重发失败批次。

## 3.7 ADM-CONFIG 线框核对

### 布局区块
- 配置分组导航。
- 参数表单区。
- diff 预览区。
- 发布与回滚操作区。

### 交互清单
- 保存草稿。
- 提交审批。
- 发布。
- 回滚到稳定版本。

## 3.8 ADM-AB 线框核对

### 布局区块
- 实验列表。
- 分流配置区。
- 指标看板区。

### 交互清单
- 开启/暂停实验。
- 紧急全局总开关。

## 3.9 ADM-LOG 线框核对

### 布局区块
- 操作日志表格。
- 高级筛选条（角色、时间、模块、操作）。
- 详情抽屉（变更前后对比）。

### 交互清单
- 导出日志。
- 快速定位高危操作。

## 3.10 后台页面验收勾选表

| 页面ID | 结构线框 | 高保真 | 状态图 | 权限标注 | 异常流 | 开发对齐 |
|---|---|---|---|---|---|---|
| ADM-DASH | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| ADM-USERS | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| ADM-AUDIT | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| ADM-PUSH | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| ADM-CONFIG | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| ADM-AB | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |
| ADM-LOG | ☐ | ☐ | ☐ | ☐ | ☐ | ☐ |

---

## 4. 设计到开发对齐检查单

## 4.1 iOS 对齐检查

- 页面路由与入参是否和附录一致。
- 组件命名是否与设计稿图层命名一致。
- 所有页面状态图是否已实现并可触发。
- 手势冲突（下拉关闭与滚动）是否完成真机验证。
- 键盘避让、安全区适配是否通过 iPhone 小屏和大屏验证。

## 4.2 后台对齐检查

- 所有高危按钮均有二次确认和权限校验。
- 表格空态、错误态、加载态统一。
- 角色权限矩阵是否与 PRD 保持一致。
- 审计日志是否覆盖关键操作链路。

## 4.3 评审节奏建议

| 节点 | 目标产物 | 参与角色 |
|---|---|---|
| 线框评审 | 结构和流程确认 | 产品/设计/前后端 |
| 视觉评审 | 视觉 token 和组件统一 | 设计/客户端/后台前端 |
| 交互评审 | 手势、状态、异常流 | 设计/客户端/测试 |
| 联调评审 | 页面数据绑定与错误态 | 客户端/后端/测试 |

---

*状态：可直接用于页面拆解、设计排期与开发联调清单。*
